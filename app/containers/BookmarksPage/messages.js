/*
 * BookmarkPage Messages
 *
 * This contains all the text for the BookmarkPage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.BookmarkPage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "Мои закладки",
  },
});
