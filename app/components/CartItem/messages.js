/*
 * CartItem Messages
 *
 * This contains all the text for the CartItem component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.CartItem";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the CartItem component!",
  },
});
