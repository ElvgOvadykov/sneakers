/*
 * SneakersCard Messages
 *
 * This contains all the text for the SneakersCard component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.SneakersCard";

export default defineMessages({
  price: {
    id: `${scope}.label.price`,
    defaultMessage: "Цена:",
  },
});
